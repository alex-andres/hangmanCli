{
  "name": "hangmancli",
  "version": "1.0.0",
  "description": "Hangmand game that uses the command line for user interaction",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node bin/dev",
    "clean": "rm -rf dist",
    "build": "npm run clean && mkdir dist && babel game -s -d dist",
    "production": "npm run build && node bin/production"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/c-andres/hangmanCli.git"
  },
  "author": "Alex Andres",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/c-andres/hangmanCli/issues"
  },
  "homepage": "https://github.com/c-andres/hangmanCli#readme",
  "dependencies": {
    "inquirer": "^3.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "babel-register": "^6.26.0"
  }
}
